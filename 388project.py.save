import RPi.GPIimport time

GPIO.setmode(GPIO.BCM)
GPIO.setup(23, GPIO.IN, pull_up_down=GPIO.PUD_UP)

while True:
	inputstate = GPIO.input(23)
	if inputstate == False:
		print('button pressed')
		time.sleep(0.2)
